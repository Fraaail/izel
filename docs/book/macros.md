# Macros Overview

Macro behavior currently spans:
- language overview documentation in `docs/project_overview.md` section 24,
- parser/lowering behavior tested in `crates/izel_parser` and `crates/izel_ast_lower`.

As the dedicated macros chapter grows, use these references:
- `docs/book/01-getting-started.md` for practical toolchain context,
- `docs/spec/02-syntax.md` for syntax and placement rules.
