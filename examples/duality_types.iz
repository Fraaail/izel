draw std::io

dual shape JsonFormat<T> {
    forge encode(&self, val: &T) -> String {
        "encoded-json"
    }
}

dual shape BinaryProtocol<T> {
    forge write(&self, val: &T) -> void !io {
        println("Writing binary data")
    }
}

forge main() {}
