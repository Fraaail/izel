draw std/io
draw std::collections::*

forge main() -> int {
    println("095: small business kpi board")

    let mut v = Vec::new()
    v.push(20)
    v.push(2)
    let size = v.len()

    let mut map = HashMap::new()
    map.insert("risk", 355)

    let mut tree = BTreeMap::new()
    tree.insert(230, "node")

    println_int(size)
    give 0
}
