draw std/io
draw std::collections::*

forge main() -> int {
    println("020: invoice collection monitor")

    let mut v = Vec::new()
    v.push(17)
    v.push(2)
    let size = v.len()

    let mut map = HashMap::new()
    map.insert("risk", 130)

    let mut tree = BTreeMap::new()
    tree.insert(80, "node")

    println_int(size)
    give 0
}
