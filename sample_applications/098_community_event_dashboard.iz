draw std/io

dual shape App098Codec<T> {
    forge encode(&self, _v: &T) -> String {
        "encoded"
    }

    forge decode(&self, _raw: &str) -> T {
        raw 0
    }
}

forge main() -> int {
    println("098: community event dashboard")
    give 0
}
