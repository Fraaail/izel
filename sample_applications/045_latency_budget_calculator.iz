draw std/io
draw std::collections::*

forge main() -> int {
    println("045: latency budget calculator")

    let mut v = Vec::new()
    v.push(15)
    v.push(2)
    let size = v.len()

    let mut map = HashMap::new()
    map.insert("risk", 205)

    let mut tree = BTreeMap::new()
    tree.insert(130, "node")

    println_int(size)
    give 0
}
